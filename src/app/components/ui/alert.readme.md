# Alert

Source: `src/app/components/ui/alert.tsx` (shadcn/ui-style primitives).

## Exports

- `Alert`
- `AlertTitle`
- `AlertDescription`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/alert`).

```tsx
import { Alert, AlertTitle, AlertDescription } from '@/app/components/ui/alert';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/alert) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



