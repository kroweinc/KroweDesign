# Toggle

Source: `src/app/components/ui/toggle.tsx` (shadcn/ui-style primitives).

## Exports

- `Toggle`
- `toggleVariants`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/toggle`).

```tsx
import { Toggle, toggleVariants } from '@/app/components/ui/toggle';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/toggle) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



