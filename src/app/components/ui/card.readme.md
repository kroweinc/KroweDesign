# Card

Source: `src/app/components/ui/card.tsx` (shadcn/ui-style primitives).

## Exports

- `Card`
- `CardHeader`
- `CardFooter`
- `CardTitle`
- `CardAction`
- `CardDescription`
- `CardContent`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/card`).

```tsx
import { Card, CardHeader, CardFooter, CardTitle } from '@/app/components/ui/card';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/card) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



