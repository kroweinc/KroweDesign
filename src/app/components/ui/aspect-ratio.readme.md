# Aspect Ratio

Source: `src/app/components/ui/aspect ratio.tsx` (shadcn/ui-style primitives).

## Exports

- `AspectRatio`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/aspect ratio`).

```tsx
import { AspectRatio } from '@/app/components/ui/aspect ratio';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/aspectratio) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



