# Table

Source: `src/app/components/ui/table.tsx` (shadcn/ui-style primitives).

## Exports

- `Table`
- `TableHeader`
- `TableBody`
- `TableFooter`
- `TableHead`
- `TableRow`
- `TableCell`
- `TableCaption`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/table`).

```tsx
import { Table, TableHeader, TableBody, TableFooter } from '@/app/components/ui/table';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/table) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



