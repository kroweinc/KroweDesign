# Input

Source: `src/app/components/ui/input.tsx` (shadcn/ui-style primitives).

## Exports

- `Input`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/input`).

```tsx
import { Input } from '@/app/components/ui/input';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/input) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



