# Resizable

Source: `src/app/components/ui/resizable.tsx` (shadcn/ui-style primitives).

## Exports

- `ResizablePanelGroup`
- `ResizablePanel`
- `ResizableHandle`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/resizable`).

```tsx
import { ResizablePanelGroup, ResizablePanel, ResizableHandle } from '@/app/components/ui/resizable';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/resizable) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



