# Tabs

Source: `src/app/components/ui/tabs.tsx` (shadcn/ui-style primitives).

## Exports

- `Tabs`
- `TabsList`
- `TabsTrigger`
- `TabsContent`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/tabs`).

```tsx
import { Tabs, TabsList, TabsTrigger, TabsContent } from '@/app/components/ui/tabs';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/tabs) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



