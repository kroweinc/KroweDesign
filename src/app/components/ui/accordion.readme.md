# Accordion

Source: `src/app/components/ui/accordion.tsx` (shadcn/ui-style primitives).

## Exports

- `Accordion`
- `AccordionItem`
- `AccordionTrigger`
- `AccordionContent`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/accordion`).

```tsx
import { Accordion, AccordionItem, AccordionTrigger, AccordionContent } from '@/app/components/ui/accordion';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/accordion) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



