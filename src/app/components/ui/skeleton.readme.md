# Skeleton

Source: `src/app/components/ui/skeleton.tsx` (shadcn/ui-style primitives).

## Exports

- `Skeleton`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/skeleton`).

```tsx
import { Skeleton } from '@/app/components/ui/skeleton';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/skeleton) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



