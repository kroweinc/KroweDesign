# Avatar

Source: `src/app/components/ui/avatar.tsx` (shadcn/ui-style primitives).

## Exports

- `Avatar`
- `AvatarImage`
- `AvatarFallback`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/avatar`).

```tsx
import { Avatar, AvatarImage, AvatarFallback } from '@/app/components/ui/avatar';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/avatar) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



