# Toggle Group

Source: `src/app/components/ui/toggle group.tsx` (shadcn/ui-style primitives).

## Exports

- `ToggleGroup`
- `ToggleGroupItem`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/toggle group`).

```tsx
import { ToggleGroup, ToggleGroupItem } from '@/app/components/ui/toggle group';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/togglegroup) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



