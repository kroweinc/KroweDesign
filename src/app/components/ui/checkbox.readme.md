# Checkbox

Source: `src/app/components/ui/checkbox.tsx` (shadcn/ui-style primitives).

## Exports

- `Checkbox`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/checkbox`).

```tsx
import { Checkbox } from '@/app/components/ui/checkbox';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/checkbox) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



