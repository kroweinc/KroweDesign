# Collapsible

Source: `src/app/components/ui/collapsible.tsx` (shadcn/ui-style primitives).

## Exports

- `Collapsible`
- `CollapsibleTrigger`
- `CollapsibleContent`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/collapsible`).

```tsx
import { Collapsible, CollapsibleTrigger, CollapsibleContent } from '@/app/components/ui/collapsible';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/collapsible) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



