# Button

Source: `src/app/components/ui/button.tsx` (shadcn/ui-style primitives).

## Exports

- `Button`
- `buttonVariants`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/button`).

```tsx
import { Button, buttonVariants } from '@/app/components/ui/button';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/button) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



