# Slider

Source: `src/app/components/ui/slider.tsx` (shadcn/ui-style primitives).

## Exports

- `Slider`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/slider`).

```tsx
import { Slider } from '@/app/components/ui/slider';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/slider) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



