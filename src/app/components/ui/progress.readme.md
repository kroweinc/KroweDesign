# Progress

Source: `src/app/components/ui/progress.tsx` (shadcn/ui-style primitives).

## Exports

- `Progress`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/progress`).

```tsx
import { Progress } from '@/app/components/ui/progress';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/progress) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



