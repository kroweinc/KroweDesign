# Separator

Source: `src/app/components/ui/separator.tsx` (shadcn/ui-style primitives).

## Exports

- `Separator`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/separator`).

```tsx
import { Separator } from '@/app/components/ui/separator';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/separator) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



