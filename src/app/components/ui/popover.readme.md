# Popover

Source: `src/app/components/ui/popover.tsx` (shadcn/ui-style primitives).

## Exports

- `Popover`
- `PopoverTrigger`
- `PopoverContent`
- `PopoverAnchor`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/popover`).

```tsx
import { Popover, PopoverTrigger, PopoverContent, PopoverAnchor } from '@/app/components/ui/popover';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/popover) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



