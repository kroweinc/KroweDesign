# Tooltip

Source: `src/app/components/ui/tooltip.tsx` (shadcn/ui-style primitives).

## Exports

- `Tooltip`
- `TooltipTrigger`
- `TooltipContent`
- `TooltipProvider`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/tooltip`).

```tsx
import { Tooltip, TooltipTrigger, TooltipContent, TooltipProvider } from '@/app/components/ui/tooltip';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/tooltip) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



