# Scroll Area

Source: `src/app/components/ui/scroll area.tsx` (shadcn/ui-style primitives).

## Exports

- `ScrollArea`
- `ScrollBar`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/scroll area`).

```tsx
import { ScrollArea, ScrollBar } from '@/app/components/ui/scroll area';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/scrollarea) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



