# Label

Source: `src/app/components/ui/label.tsx` (shadcn/ui-style primitives).

## Exports

- `Label`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/label`).

```tsx
import { Label } from '@/app/components/ui/label';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/label) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



