# Radio Group

Source: `src/app/components/ui/radio group.tsx` (shadcn/ui-style primitives).

## Exports

- `RadioGroup`
- `RadioGroupItem`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/radio group`).

```tsx
import { RadioGroup, RadioGroupItem } from '@/app/components/ui/radio group';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/radiogroup) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



