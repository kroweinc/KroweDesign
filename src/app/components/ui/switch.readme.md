# Switch

Source: `src/app/components/ui/switch.tsx` (shadcn/ui-style primitives).

## Exports

- `Switch`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/switch`).

```tsx
import { Switch } from '@/app/components/ui/switch';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/switch) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



