# Input Otp

Source: `src/app/components/ui/input otp.tsx` (shadcn/ui-style primitives).

## Exports

- `InputOTP`
- `InputOTPGroup`
- `InputOTPSlot`
- `InputOTPSeparator`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/input otp`).

```tsx
import { InputOTP, InputOTPGroup, InputOTPSlot, InputOTPSeparator } from '@/app/components/ui/input otp';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/inputotp) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



