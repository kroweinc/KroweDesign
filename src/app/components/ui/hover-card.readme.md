# Hover Card

Source: `src/app/components/ui/hover card.tsx` (shadcn/ui-style primitives).

## Exports

- `HoverCard`
- `HoverCardTrigger`
- `HoverCardContent`

## Install / import

Use path aliases as configured in this repo (for example `@/app/components/ui/hover card`).

```tsx
import { HoverCard, HoverCardTrigger, HoverCardContent } from '@/app/components/ui/hover card';
```

## Usage

Compose Radix-style parts: trigger + content (or root + children) as in the [shadcn documentation](https://ui.shadcn.com/docs/components/hovercard) — adjust slug if the upstream name differs.

Styling uses Tailwind utility classes and `cn()` from `@/app/components/ui/utils`.



